# X (Twitter) thread · English · 4 tweets

Each tweet under 280 chars. Attach images as noted.

---

## Tweet 1 (hook — attach **slide1.png**)

```
Same gpt-image-2 API.
Two totally different results.

The difference is 3 markdown files.

A thread on what "agent-mediated prompting" actually means in code 🧵
```

(248 chars)

---

## Tweet 2 (decompose — attach **slide3.png** only)

```
The right image isn't a filter. It's gpt-image-2 + a markdown-defined prompt: photo anchors PRESERVED, gongbi elements painted INTO the scene as discrete objects.

Vulca then decomposes the result into 9 named entities + 1 residual via YOLO + DINO + SAM.
```

(265 chars)

---

## Tweet 3 (redraw — attach **slide4.png**)

```
Once decomposed, you can redraw any layer.

Same lanterns layer → prompt "朱砂 +15%, 三矾九染 depth richer" → gpt-image-2 returns a full gongbi reinterpretation.

Vulca exposes both paths (alpha-iso + LLM redraw) via MCP. The agent picks per intent.
```

(258 chars)

---

## Tweet 4 (verdict trail + CTA — attach **slide5.png** + **slide6.png**)

```
plan.md verdict trail: L2=0.65 hard-fails 0.70 (single-pass diffusion can't simulate 三矾九染 multi-wash physics — category ceiling). Strict rubric: reject. Maintainer: user-override-accept. Both archived.

Open source: github.com/vulca-org/vulca-visual-control-sdk
```

(268 chars)

---

## Notes

- Tweet 1: fire alone first, observe engagement, then thread.
- Tweet 2 attaches 2 images (slide 2 hero + slide 3 decompose grid). X allows up to 4 per tweet.
- Tweet 4 attaches slide 5 + slide 6.
- All non-English glyphs (朱砂, 三矾九染, 千里江山图) render fine on X — no surrogate-pair issues.
- If quote-tweeting from a Vulca-org account, also tag `@OpenAI` (model is theirs) — judgment call, not required.
- Best post time: weekdays 14:00 UTC for max EU + US east-coast overlap.

## Hook variations to A/B test

If T1 underperforms after 2h, retry with:

```
3 markdown files turned a Glasgow street photo into a Song-dynasty gongbi cross-cultural carousel — using the same gpt-image-2 API as a "naive prompt" baseline.

Walking through what changed 🧵
```

(238 chars)

Or:

```
"Just give the model a better prompt" undersells what's happening when an agent walks proposal.md → design.md → plan.md before the API call.

Six slides, same model, two outcomes 🧵
```

(190 chars)
